# Быстрый старт AmneziaWG Bot

## 1. Создание Telegram бота

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `AmneziaWG Config Bot`)
4. Введите username бота (например: `amneziabot`)
5. Скопируйте полученный токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## 2. Получение Telegram ID

1. Откройте Telegram и найдите @userinfobot
2. Нажмите `/start`
3. Бот отправит вам ваш ID (например: `123456789`)
4. Запросите ID у других пользователей, которым нужен доступ

## 3. Настройка .env файла

```bash
cd /opt/AmneziaBot
cp .env.example .env
nano .env
```

Заполните обязательные поля:

```env
# Вставьте токен от BotFather
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Ваш Telegram ID (админ)
ADMIN_ID=123456789

# Список разрешенных пользователей (через запятую)
USERS=123456789,987654321,112233445
```

Сохраните файл: `Ctrl+O`, `Enter`, `Ctrl+X`

## 4. Запуск бота

### Вариант 1: Запуск вручную (для тестирования)

```bash
cd /opt/AmneziaBot
source venv/bin/activate
python main.py
```

Для остановки нажмите `Ctrl+C`

### Вариант 2: Запуск как системный сервис (рекомендуется)

```bash
# Включить автозапуск
sudo systemctl enable amneziabot

# Запустить бот
sudo systemctl start amneziabot

# Проверить статус
sudo systemctl status amneziabot

# Просмотр логов
sudo journalctl -u amneziabot -f

# Остановить бот
sudo systemctl stop amneziabot

# Перезапустить бот
sudo systemctl restart amneziabot
```

## 5. Использование бота

1. Откройте Telegram и найдите вашего бота
2. Нажмите `/start`
3. Выберите тип устройства:
   - 📱 Для телефона
   - 💻 Для ноутбука
   - 🌐 Для роутера
4. Получите конфигурационный файл
5. Импортируйте файл в приложение AmneziaWG

## 6. Админ-функции

Для администратора доступны дополнительные команды:

- `/stats` или кнопка "📊 Статистика" - статистика использования
- `/users` или кнопка "👥 Пользователи" - список пользователей

## 7. Добавление нового пользователя

1. Получите Telegram ID пользователя (через @userinfobot)
2. Откройте файл .env:
   ```bash
   nano /opt/AmneziaBot/.env
   ```
3. Добавьте ID в переменную USERS (через запятую):
   ```env
   USERS=123456789,987654321,112233445,999888777
   ```
4. Перезапустите бота:
   ```bash
   sudo systemctl restart amneziabot
   ```

## 8. Проверка работы

```bash
# Проверить, что бот запущен
sudo systemctl status amneziabot

# Проверить логи на ошибки
tail -f /opt/AmneziaBot/logs/bot.log

# Проверить базу данных
ls -lh /opt/AmneziaBot/data/database.db

# Проверить AmneziaWG контейнер
docker ps | grep amnezia-awg
```

## 9. Troubleshooting

### Бот не отвечает

```bash
# Проверить статус
sudo systemctl status amneziabot

# Посмотреть логи
sudo journalctl -u amneziabot -n 50 --no-pager

# Перезапустить
sudo systemctl restart amneziabot
```

### Ошибка "BOT_TOKEN not found" или "Отсутствуют обязательные переменные окружения"

Убедитесь, что файл `.env` создан и заполнен правильно:

```bash
cat /opt/AmneziaBot/.env
```

Должны быть заполнены все обязательные поля:
- BOT_TOKEN
- ADMIN_ID
- SERVER_ENDPOINT
- SERVER_PUBLIC_KEY
- PRESHARED_KEY

### Ошибка "docker: not found"

Если в логах видите эту ошибку, проверьте systemd сервис:

```bash
# Проверить содержимое сервиса
cat /etc/systemd/system/amneziabot.service | grep PATH

# Должно быть:
# Environment="PATH=/opt/AmneziaBot/venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"

# Если PATH неправильный, перезагрузите конфигурацию
sudo systemctl daemon-reload
sudo systemctl restart amneziabot
```

### Ошибка "RuntimeError: threads can only be started once"

Эта проблема исправлена в текущей версии. Если столкнулись с ней:

```bash
# Убедитесь, что используете последний код
cd /opt/AmneziaBot
git pull

# Перезапустите бота
sudo systemctl restart amneziabot
```

### Пользователь не может получить доступ

1. Проверьте, что ID добавлен в USERS:
   ```bash
   cat /opt/AmneziaBot/.env | grep USERS
   ```
2. Перезапустите бота после изменений:
   ```bash
   sudo systemctl restart amneziabot
   ```

### AmneziaWG не работает

```bash
# Проверить контейнер
docker ps | grep amnezia-awg

# Проверить логи контейнера
docker logs amnezia-awg

# Перезапустить контейнер
docker restart amnezia-awg
```

## 10. Остановка и удаление

```bash
# Остановить бота
sudo systemctl stop amneziabot
sudo systemctl disable amneziabot

# Удалить сервис (опционально)
sudo rm /etc/systemd/system/amneziabot.service
sudo systemctl daemon-reload

# Удалить проект (опционально)
rm -rf /opt/AmneziaBot
```

## Полезные ссылки

- Документация AmneziaVPN: https://amnezia.org/
- Документация python-telegram-bot: https://python-telegram-bot.org/
- Исходный код проекта: `/opt/AmneziaBot/`

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `tail -f /opt/AmneziaBot/logs/bot.log`
2. Проверьте системные логи: `sudo journalctl -u amneziabot -n 100`
3. Прочитайте README.md для подробной информации

